import React from 'react';
import styles from './about.text.block.module.scss';

type Props = {};

export default function AboutTextBlock({}: Props) {
  return <div className={styles.textBlock}>TExt block</div>;
}
